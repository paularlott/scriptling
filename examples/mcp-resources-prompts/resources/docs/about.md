# About This Server

This is a **static resource**: a plain Markdown file dropped into the
`--mcp-resources` directory. It is served verbatim at the URI derived from its
path — `docs://about.md` (the top-level directory becomes the URI scheme, the
rest of the path becomes the URI).

No metadata, no script. Files just work.
