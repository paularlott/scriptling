# App Bundle Guide

This is the documentation for the app-bundle example package.

## Overview

The app-bundle demonstrates the complete package format: HTTP routes, MCP
tools, and static web assets — all from a single manifest-declared bundle.
